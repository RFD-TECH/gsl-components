node_modules
dist
coverage
# Machine-generated by scripts/generate-theme-tokens.mjs (npm run generate:tokens,
# also runs via the "prepare" hook and as the first step of "build").
src/generated/
*.local
.local.*
CHANGELOG-draft.md
PLAN.md
.DS_Store
.playwright-mcp/
llms.txt
table-demo.png
*.tgz
*.tgz.bak
bun.lock
AGENTS.md
git.md
WCAG-AUDIT.md
# bun
bun.lockb
.bun/
.bun-cache/
.bun-cache-temp/
.bun-cache-temp-*/
.bun-cache-temp-*/
# pnpm is the only install path here; npm/yarn locks drift and get scanned separately.
package-lock.json
**/package-lock.json
yarn.lock

# Agent config: generated by `rfdui setup` on postinstall, machine-specific, never shared
.mcp.json
.cursor/
opencode.json
.ai/
